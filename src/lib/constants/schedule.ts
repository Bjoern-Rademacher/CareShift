import type { Weekday } from "@/types/common";
import { ShiftGroup } from "@/types/view";

export const WEEKDAYS: Weekday[] = [
  "MON",
  "TUE",
  "WED",
  "THU",
  "FRI",
  "SAT",
  "SUN",
];

export const SHIFT_GROUPS: ShiftGroup[] = ["MORNING", "EVENING", "NIGHT"];
